# ADR 003: DTCG design tokens

Status: Accepted by Step 5.

Store primitive, semantic, and component tokens in DTCG-compatible JSON and generate CSS custom properties. A small dependency-free generator makes the pipeline reproducible and fails validation when committed CSS is stale.
