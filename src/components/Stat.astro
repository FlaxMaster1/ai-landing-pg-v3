---
interface Props { value: string; label: string; detail?: string; }
const { value, label, detail } = Astro.props;
---
<div class="c-stat">
  <p class="c-stat__value">{value}</p>
  <p class="c-stat__label">{label}</p>
  {detail && <p class="c-stat__detail">{detail}</p>}
</div>
